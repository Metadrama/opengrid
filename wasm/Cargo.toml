[package]
name = "opengrid-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
rand = { version = "0.8", features = ["small_rng"] }
rand_pcg = "0.3"

[profile.release]
opt-level = "s"  # Optimize for size
lto = true
